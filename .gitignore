node_modules
dist
dev-dist
*.log

# Test/build artifacts — always regenerable, never commit
coverage/
.nyc_output/
# Ignore all .env files with real secrets — NEVER commit these
.env
.env.local
.env.production
.env.production.local
# Explicitly allow example templates (must NOT be ignored)
!.env.example
!**/.env.example
.DS_Store
.idea/workspace.xml

# Secrets — never commit these
google-service-account.json
*.p8
*.pem
*.key
/infra/coturn/turnserver.conf.rendered

aegislink-server.zip
*.zip
*.tar.gz
desktop/node_modules/
server/node_modules/
promo-video/node_modules/

# Scratch / debug artifacts (screenshots, UI dumps, logs, ad-hoc scripts).
# Rule: anything transient lives under _scratch/ and is never committed.
/_scratch/
/_*.png
/_*.xml
/.log_*
/.tmp_*
/.raw_*
/bugreport-*/
*.apk

# Internal session handoff — contains infra/access details, keep out of history
docs/SESSION_HANDOFF.md

# Transient review/debug dumps — regenerable, never commit (Hito 0 cleanup)
alerts.json
all_alerts.json
pr_*_comments.txt
mobile/crash2.txt
server/.expo/

# Signing keystore backup + ad-hoc test scripts — never commit
/_keystore_backup/
/_powtest.mjs

# Runtime upload dir — transient blobs, never commit (relay writes here)
/uploads/

# EAS local signing credentials (never commit)
mobile/credentials.json
